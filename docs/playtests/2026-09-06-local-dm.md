# Local AI DM playtest — 2026-09-06

Result: **FAIL — scene progression blocked on the second player action.**

## Setup

- Source: `022ffbe` (tree identical to PR #360 head `c17b499`).
- Fresh backend: http://127.0.0.1:8001, `DM_INLINE_EXECUTE=1`.
- Fresh frontend: http://localhost:3001, connected to that backend.
- Real Supabase password login/JWT; configured database and real model providers. This was a local application test, not an isolated local database.
- DM: Meta `muse-spark-1.3-contributor`; narrator: OpenAI `gpt-5.6-luna`.
- Campaign: `9d350ba2-46e4-4949-be71-ea1c0304e000`, “Astra local playtest — 2026-09-06”.
- Character: Mara Reed — Astra playtest, level 1 human rogue.
- Existing campaign was not modified.
- API playtest only: browser automation was unavailable, so visual refresh/rendering and actual realtime client behavior were not verified.

An existing backend on port 8000 had been running since the previous day. Initial bootstrap there created the campaign but left its opening prepared. A fresh backend from this checkout completed the same opening through a repeated start command, preserving its turn ID. Disk source equivalence does not establish what modules the old process had loaded.

## Results

| Check | Result | Evidence |
| --- | --- | --- |
| Real authentication, character creation, selection/readiness | Pass | Normal authenticated API routes succeeded. |
| Opening and repeated start | Pass with latency | One opening turn/message; fresh backend request took 56.24 seconds, including validator regeneration. |
| Conversation | Fail for gameplay quality | 61.54 seconds; DM echoed the question without an answer, response from the room, or new information. |
| Observation / possible Perception roll | Fail | 95.54 seconds; four rejected generation attempts; no roll request or narration. |
| Consequential movement after failure | Blocked | HTTP 201 accepted the action, but no new DM turn was returned; prior failed turn blocks progression. |
| Reauthentication / snapshot continuity | Pass at API level | Previously seen player/DM message IDs survived a fresh login and new snapshot requests. |
| Duplicate observation submission | Pass | Same operation ID returned the same submission; exactly one player message remained. |
| Busy/error projection | Fail | Snapshot `dm_state.status` was `idle` during adjudication and after terminal failure. |
| Realtime broadcasts | Unavailable in local configuration | Server logs report missing `SUPABASE_SERVICE_ROLE_KEY`; snapshot retrieval worked. |

## Transcript

**AI DM opening:**

> It is evening on the first day. You rest at the Emberhold Tavern.
>
> Beyond the fog outside, strange lights move.
>
> What do you do?

**Player:**

> I stay inside the tavern and call toward the room: “Has anyone seen those lights before? I am Mara Reed, a road scout. I would rather learn what is out there before walking into it.” I listen for a reply.

**AI DM:**

> It is evening of the first day in the Emberhold Tavern. Strange lights move beyond the fog outside.
>
> Mara Reed stays inside the tavern and calls toward the room.
>
> “Has anyone seen those lights before? I am Mara Reed, a road scout. I would rather learn what is out there before walking into it.”
>
> Mara Reed listens for a reply.
>
> What do you do?

**Player observation:**

> I move to a window inside the Emberhold Tavern and study the moving lights through the fog, looking for their number, direction, and whether someone is carrying them. I keep my position inside. [OOC] If the fog makes this uncertain, I am ready to make the appropriate Perception check.

**Result:** No narration or roll request. Turn failed.

**Player movement:**

> I leave the Emberhold Tavern through its door and advance ten paces into the fog toward the strange lights, keeping the tavern behind me as a landmark. I stop there and listen before going farther.

**Result:** Submission accepted, no new DM turn; authoritative scene remained Emberhold Tavern. Movement was not adjudicated.

## Failure evidence and next work

Observation turn: `89f7c5a0-94ec-4656-881f-66ac8e5bdc82`.
Attempt: `56fde479-4b3c-414b-a070-f182fc1928cd`.
Trace: `114b945654f24c13aec53c70e8fe9f92`.

Validator sequence:

1. `unknown_canonical_id` ×2; `player_declaration_without_source_ref`.
2. `contract/invalid_contract`.
3. `unknown_canonical_id` ×2; `player_declaration_without_source_ref`.
4. `contract/invalid_contract`.

Persisted failure:

```text
ValidatorRejectionError: Validation failed after 4 attempts; last violations: ['invalid_contract']
```

Both turn and attempt are `failed_visible`. Subsequent movement logged `dm_turn stream_boundary_blocked`. Final snapshot contains four player messages, two completed DM messages, no roll requests, and `dm_state.status=idle`.

Priorities:

1. Diagnose model contract generation/reference grounding and why regeneration fails on ordinary observation. Keep validation intact.
2. Project pending/running/failed turn state into snapshots and expose a usable recovery path. The current snapshot implementation derives DM state only from active narration streams (`backend/app/snapshot/service.py`).
3. Investigate conversation responses collapsing into paraphrases, then rerun the scene through an actual roll and a committed movement consequence.
4. Configure realtime broadcasting and perform a browser refresh/stream recovery test.

No roll fulfillment or successful consequential action was achieved; these remain blocked, not passed. No application fixes were made in this test.

Raw API evidence and server logs are in `/tmp/dnd-playtest-astra/` on the test machine; these are temporary local artifacts, not committed fixtures. The local frontend/backend were left running for inspection.
